apiVersion: v1
kind: Service
metadata:
  name: products-service      # ← Orders API calls http://products-service
spec:
  selector:
    app: products-api
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
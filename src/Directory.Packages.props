<Project>
  <!--
    Single source of truth lives at the repo root: /Directory.Packages.props.
    This stub forwards so projects under src/ inherit those versions.
  -->
  <Import Project="$(MSBuildThisFileDirectory)..\Directory.Packages.props" />
</Project>

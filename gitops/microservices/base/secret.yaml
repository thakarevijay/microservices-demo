# NOTE: this is a placeholder secret committed to git for now.
# In M4 we'll replace this with External Secrets Operator backed by Civo's
# secret store (or move to AWS/GCP secret manager). DO NOT put real secrets here.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: microservices
type: Opaque
stringData:
  API_KEY: demo-secret-key
